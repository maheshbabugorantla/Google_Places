<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <stroke
        android:color="@color/transparent"
        android:width="1dp"
        />
    <!-- using transparent color in stroke to match the margin color with the theme color -->
    <solid android:color="@color/transparent"/>
    <!-- when the button be pressed, the button will show up the same color as the theme color -->
    

</shape>